There are two pipelines here: `influenza` and `ebola`. 

To run each make sure that you have a conda environment with `snakemake` installed then run:
```shell
snakemake -f influenza
snakemae -f ebola 
```